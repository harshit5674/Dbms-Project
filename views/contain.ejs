<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>STYLEMART</title>
    <link  href="css/style.css"  rel="stylesheet" type="text/css" />
</head>
<body>
    <!--Header-->
    <header id="header">
        <nav>
            <div class="container">
                <div class="text-center">
                    <a href="/customer" class="nav-brand text-dark">Clothes Management System</a>
                </div>
            </div>
        </nav>
    </header>

<main id="int-main">
    <div class="container">
        <div class="box-nav d-flex justify-between">
            <a href="/add-contain?id=<%=tid%>" class="border-shadow">
                <span class="text-gradient">New customer</span>
            </a>
        </div>
        <br>
        <form action="/contain" method="POST">
            <table class="table">
                <thead class="thead-dark">
                    <tr>
                        <th>Transaction ID</th>
                        <th>Product ID</th>
                        <th>Quantity</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% for(var i=0;i < contains.length ;i++){%>
                    <tr>
                        <td><%=contains[i].tid%></td>
                        <td><%=contains[i].pid%></td>
                        <td><%=contains[i].quantity%></td>
                        <td>
                            <a href="/update_contain?id=<%=contains[i].id%>" class="btn border-shadow update">
                                <span class="text-gradient"></span>
                            </a>
                            <a class="btn border-shadow delete" data-id=<%=contains[i].id%>>
                                <span class="text-gradient"></span>
                            </a>
                        </td>
                    </tr>
                    <%}%>
                </tbody>
            </table>
        </form>
    </div>
</main>

<%-include('include/_footer')%>